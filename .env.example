# Enables AI interpretation of drawings (Claude Opus 4.8 vision) in paint.exe.
# Copy to `.env` and add your key. WITHOUT it, drawings still come to life via
# a local pixel-analysis heuristic — the AI path just makes them smarter.
# The key is read only by the Vite dev server and never sent to the browser.
ANTHROPIC_API_KEY=
